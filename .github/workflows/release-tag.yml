on:
    push:
        tags:
            - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

name: Create Release

permissions: {}
jobs:
    build:
        permissions:
            contents: write # to create release via gh CLI

        name: Create Release
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v5

            - name: Create Release for Tag
              env:
                  GH_TOKEN: ${{ secrets.RELEASE_TAG_GITHUB_TOKEN }}
              run: |
                  gh release create "${{ github.ref_name }}" \
                    --repo "${{ github.repository }}" \
                    --title "${{ github.ref_name }}" \
                    --prerelease="${{ contains(github.ref, 'beta') }}" \
                    --notes "Please refer to [CHANGELOG.md](https://github.com/volverjs/zod-vue-i18n/blob/${{ contains(github.ref, 'beta') && 'develop' || 'main' }}/CHANGELOG.md) for details."
